'use client';

import { createTheme } from '@mui/material';

const theme = createTheme({
  cssVariables: true,
});

// const theme = createTheme({
//   palette: {
//     primary: {
//       main: orange[500],
//       light: red[600],
//     },
//   },
// });

export default theme;
